import {combineReducers} from 'redux';import {FETCH_USER_DATA, VERIFY_TOKEN, UPDATE_BALANCE, EXCHANGE} from "./actions";export function data(state = {    verifyToken: null,    userData: null}, action) {    switch (action.type) {        case VERIFY_TOKEN:            return {                ...state,                verifyToken: action.verifyToken,            };        case FETCH_USER_DATA:            return {                ...state,                userData: action.userData            };        case UPDATE_BALANCE:            return {                ...state,                userData: action.userBalance            };        case EXCHANGE:            return {                ...state,                userData:action.userExchange            };        default:            return state;    }}const rootReducer = combineReducers({    data});export default rootReducer;